<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EXAMEN_PROM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="android:textColor">@color/my_light_text</item> <!-- Color de texto en modo claro -->
        <item name="android:textColorPrimary">@color/my_light_text</item> <!-- Color de texto primario en modo claro -->
        <item name="android:textColorSecondary">@color/my_light_text</item> <!-- Color de texto secundario en modo claro -->
    </style>

    <style name="Theme.EXAMEN_PROM" parent="Base.Theme.EXAMEN_PROM" />
</resources>